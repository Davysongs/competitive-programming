// Minimal C++20 competitive-programming template.

#include <iostream>

void solve() {
    // Parse the problem-specific input and write the answer.
}

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    solve();
    return 0;
}
